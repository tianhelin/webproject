<h2>分類管理</h2>
<div class="posttype-list clearfix">
  <% @posttypes.each do |posttype| %>
    <div class="col-xs-4">
      <%= posttype.typename %>
    </div>
    <div class="col-xs-8">
      <% if posttype.classroom_id == 0 && posttype.id > 2 %>
        <%= link_to '刪除分類', posttype_path(posttype), :method => :delete, :data => { :confirm => "確定刪除? 將會連同此分類之文章一起刪除" }, :class => "btn btn-primary" %>
      <% else %>
        <p>不能被刪除的分類</p>
      <% end %>
    </div>
  <% end %>
</div>

<div class="form-div">
  <%= form_for @posttype do |f| %>
  <div class="input-group col-xs-6">
    <%= f.text_field :typename,:class => 'form-control' %>
    <span class="input-group-btn">
      <%= f.submit "新增分類",:class => 'btn btn-primary' %>
    </span>
  </div>
</div>
<% end %>