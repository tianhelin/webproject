<h1>修改課程資訊</h1>
<%= form_for @classroom do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= f.submit '更新課程資訊', :class => 'btn btn-default' %>
<% end %>

<%= link_to '回課程總覽', classrooms_path, :class => 'btn btn-default' %>
