<h1>hihi</h1>
<table>
  <tr>
  <% @post.each do |p| %>
    <td>
      <%= p.topic %>
    </td>
    <td>
      <%= link_to '閱讀', post_path(p) %>
      <%= link_to '編輯', edit_post_path(p) %>
      <%= link_to '刪除', posts_path %>
    </td>
  </tr>
  <% end %>
</table>

<%= link_to '新增文章', new_post_path %>
